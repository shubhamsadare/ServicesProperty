server:
  port: 9999
spring:
  cloud:
    config:
      server:
        git:
          uri: git@github.com:shubhamsadare/ConfigRepo.git
          default-label: main
          clone-on-start: true
        bootstrap: true
      enabled: true
   
  application:
    name: Config-Server
config:
  user: User Hello Dev from config server
  role: -role is Development from config
